package com.here.sdk.core.engine;

import android.content.Context;
import androidx.annotation.NonNull;

interface CAresInitialiserBridge {
    void extractConnectivityManager(@NonNull Context context);
}
