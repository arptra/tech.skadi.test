package androidx.cardview.widget;

import android.content.Context;
import android.content.res.ColorStateList;

interface CardViewImpl {
    float a(CardViewDelegate cardViewDelegate);

    float b(CardViewDelegate cardViewDelegate);

    float c(CardViewDelegate cardViewDelegate);

    float d(CardViewDelegate cardViewDelegate);

    void e(CardViewDelegate cardViewDelegate);

    float f(CardViewDelegate cardViewDelegate);

    void g(CardViewDelegate cardViewDelegate, float f);

    void h(CardViewDelegate cardViewDelegate, float f);

    void i(CardViewDelegate cardViewDelegate, float f);

    ColorStateList j(CardViewDelegate cardViewDelegate);

    void k(CardViewDelegate cardViewDelegate);

    void l(CardViewDelegate cardViewDelegate, Context context, ColorStateList colorStateList, float f, float f2, float f3);

    void m(CardViewDelegate cardViewDelegate);

    void n();

    void o(CardViewDelegate cardViewDelegate, ColorStateList colorStateList);
}
